# CampusNet-Copilot 本地启动说明

本文档按本地测试顺序说明需要启动哪些服务。以下命令默认在 PowerShell 中执行，项目后端目录为：

```powershell
F:\project\CampusNet-Copilot\campusnet-backend\net
```

## 0. 端口约定
```text
NetBox:              http://localhost:8000
Grafana:             http://localhost:3000
Prometheus:          http://localhost:9090
NetBox MCP:          http://127.0.0.1:7001/mcp
Prometheus MCP:      http://localhost:9001/sse
Grafana MCP:         http://localhost:9002/sse
TimesFM MCP:         http://localhost:9003/sse
CampusNet FastAPI:   http://127.0.0.1:8010
前端测试台:           http://127.0.0.1:8010/
```

## 1. 启动基础服务

启动 Docker 后端服务(主要是Netbox和Prometheus)：

```powershell
cd F:\project\CampusNet-Copilot\campusnet-backend\net
docker compose up -d
```


## 2. 配置 NetBox MCP Server

NetBox MCP Server 位于后端同级目录：

```powershell
F:\project\CampusNet-Copilot\campusnet-backend\netbox-mcp-server
```

确认它的 `.env` 中至少包含：

```env
NETBOX_URL="http://localhost:8000"
NETBOX_TOKEN="你的_NETBOX_TOKEN"
TRANSPORT="http"
HOST="127.0.0.1"
PORT="7001"
```

注意：NetBox MCP 当前使用 Streamable HTTP，正确端点是：

```text
http://127.0.0.1:7001/mcp
```


启动 NetBox MCP Server：

```powershell
cd F:\project\CampusNet-Copilot\campusnet-backend\netbox-mcp-server
uv run netbox-mcp-server
```

如果需要显式指定参数：

```powershell
uv run netbox-mcp-server `
  --netbox-url http://localhost:8000 `
  --netbox-token 你的_NETBOX_TOKEN `
  --transport http `
  --host 127.0.0.1 `
  --port 7001
```

## 3. 启动 Prometheus MCP Server

打开新终端：

```powershell
cd F:\project\CampusNet-Copilot\campusnet-backend\net\scripts
uv run run_prometheus_mcp.py
```

## 5. 启动 TimesFM MCP Server

打开新终端：

```powershell
cd F:\project\CampusNet-Copilot\campusnet-backend\net\scripts
uv run run_timesfm_mcp.py
```

有真实 TimesFM 推理服务，额外设置：

```powershell
$env:TIMESFM_REMOTE_URL = "你的 TimesFM 推理服务地址"
$env:TIMESFM_REMOTE_TOKEN = "你的 token"
```

否则 TimesFM MCP 会使用本地 EWMA 兜底预测。

## 6. 启动 CampusNet FastAPI 后端

打开新终端：

```powershell
cd F:\project\CampusNet-Copilot\campusnet-backend\net
```

默认避免随机后台遥测事件干扰用户诊断，保持：

```powershell
$env:TELEMETRY_MOCK_ENABLED = "false"
```

启动 FastAPI：

```powershell
uv run python -m uvicorn app.app:app --host 127.0.0.1 --port 8010 --reload
```

## 7. 前端测试台

浏览器访问：

```text
http://127.0.0.1:8010/
```

前端测试台支持：

```text
1. 输入用户问题。
2. 创建后台诊断任务。
3. 展示流程状态，例如拓扑查询、指标查询、生成控制指令、安全审查。
4. 遇到需审批命令时暂停。
5. 点击确认后继续 mock SDN dry-run。
6. 展示最终诊断与修复闭环报告。
```

## 8. 常用接口测试

### 8.1 健康检查

```powershell
Invoke-RestMethod http://127.0.0.1:8010/health
```

### 8.2 用户诊断

```powershell
$res = Invoke-RestMethod `
  -Method Post `
  -Uri "http://127.0.0.1:8010/chat" `
  -ContentType "application/json" `
  -Body '{"user_id":"u1","question":"图书馆三楼 WiFi 很卡，网页打不开，帮我分析原因"}'

$res.answer
```

提取事件 ID：

```powershell
$res.answer -match 'evt-[a-f0-9]+' | Out-Null
$eventId = $matches[0]
$eventId
```

### 8.3 查看任务状态

```powershell
Invoke-RestMethod -Uri "http://127.0.0.1:8010/tasks/$eventId/status" | ConvertTo-Json -Depth 8
```

### 8.4 审批高风险命令

如果任务状态为 `waiting_approval`，执行：

```powershell
Invoke-RestMethod `
  -Method Post `
  -Uri "http://127.0.0.1:8010/tasks/$eventId/approve" `
  -ContentType "application/json" `
  -Body '{"approved_by":"admin"}'
```

### 8.5 查看最终报告

```powershell
Invoke-RestMethod -Uri "http://127.0.0.1:8010/reports/$eventId"
```

### 8.6 查看队列和存储状态

```powershell
Invoke-RestMethod http://127.0.0.1:8010/pipeline/stats
```

### 8.7 查看 MCP 连接状态

```powershell
Invoke-RestMethod http://127.0.0.1:8010/api/mcp/health | ConvertTo-Json -Depth 8
```

### 8.8 通过 FastAPI 统一调用 MCP 工具

```powershell
Invoke-RestMethod `
  -Method Post `
  -Uri "http://127.0.0.1:8010/api/mcp/call" `
  -ContentType "application/json" `
  -Body '{"server_name":"prometheus","tool_name":"instant_query","arguments":{"promql":"up"}}' |
  ConvertTo-Json -Depth 8
```

### 8.9 查询单设备实时遥测指标

```powershell
Invoke-RestMethod `
  -Method Post `
  -Uri "http://127.0.0.1:8010/api/ops/metrics" `
  -ContentType "application/json" `
  -Body '{"device_id":"AP-LIB-3F-02"}' |
  ConvertTo-Json -Depth 8
```

返回格式示例：

```json
{
  "device_id": "AP-LIB-3F-02",
  "ap_load": 92.3,
  "packet_loss": 0.061,
  "latency": 156.0,
  "bandwidth_usage": 87.2,
  "timestamp": "2026-05-29T10:20:00+00:00"
}
```

### 8.10 查看 Agent 状态流输出

```powershell
Invoke-RestMethod -Uri "http://127.0.0.1:8010/api/agent/status?event_id=$eventId" |
  ConvertTo-Json -Depth 10
```

### 8.11 单独测试 SDN Controller Adapter

当前 Adapter 使用 mock 模式，只编译控制器负载并进行 dry-run，不会修改真实网络。

```powershell
Invoke-RestMethod `
  -Method Post `
  -Uri "http://127.0.0.1:8010/sdn/dry-run" `
  -ContentType "application/json" `
  -Body '{
    "command_type":"poe_power_cycle",
    "target":"AP-LIB-3F-01",
    "command":"poe_power_cycle --ap AP-LIB-3F-01 --dry-run",
    "risk_level":"warning",
    "requires_approval":true,
    "dry_run":true,
    "rationale":"测试 SDN Adapter 编译和 dry-run"
  }' |
  ConvertTo-Json -Depth 10
```

模拟已审批命令下发：

```powershell
Invoke-RestMethod `
  -Method Post `
  -Uri "http://127.0.0.1:8010/sdn/mock-dispatch" `
  -ContentType "application/json" `
  -Body '{
    "command_type":"prepare_link_failover",
    "target":"SW-LIB-AGG-01",
    "command":"prepare_link_failover --device SW-LIB-AGG-01 --dry-run",
    "risk_level":"critical",
    "requires_approval":true,
    "dry_run":true,
    "rationale":"测试 OpenFlow failover payload 编译"
  }' |
  ConvertTo-Json -Depth 10
```
